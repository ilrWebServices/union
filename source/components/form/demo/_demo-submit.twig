{% set value = value|default('Register') %}

{% set classes = alt ? 'cu-input--submit--alt' : '' %}

{% include '@union/_form-input.twig' with {
  attributes: {
    class: classes,
    name: '[form][submit]',
    type: 'submit',
    value: value,
    id: 'form.submit'
  }
} only %}

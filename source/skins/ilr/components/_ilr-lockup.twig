{% set wrapper_attributes = {
  class: 'cu-logo cu-logo--ilr-lockup'
}%}

{% set lockup_attributes = {
  class: 'ilr__logo-image',
  width: '350',
  alt: 'Cornell University\'s ILR School',
  src: "/libraries/union/source/skins/ilr/images/ilr-cornell-lockup.svg"
}%}

{% set attributes = union_attributes(lockup_attributes) %}
{% set logo_link = logo_link|default('/')%}

{% include '@union/_logo.twig' with {
  logo_link: logo_link,
  attributes: attributes,
  wrapper_attributes: wrapper_attributes
} only %}

/**
 * Call to Action component
 */

(function (Drupal) {
  'use strict';

  Drupal.behaviors.callToAction = {
    attach: function (context, settings) {
      // Add any JavaScript functionality for the call to action component here
      // Currently, this component doesn't require any JavaScript
    }
  };

})(Drupal);
